# FullStackReactFastAPITS


In backend, start virtual environment with 
source .venv/bin/activate
then start server with
uvicorn app.main:app --reload --port 7777

In frontend, start server with 
npm run dev